mysql -uroot -p123456 -e 'use auto_aggr;truncate table auto_aggr.endpoint; insert into  auto_aggr.endpoint (select * from graph.endpoint where endpoint like "%server_%" and endpoint not like "%error-endpoint%" and  endpoint not like "%id" and  endpoint not like "%th" and  endpoint not like "%tw" and  endpoint not like "%ph" and  endpoint not like "%vn" and  endpoint not like "%sg" and  endpoint not like "%my" );'
